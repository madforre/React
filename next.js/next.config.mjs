/** @type {import('next').NextConfig} */

const nextConfig = {};

// const nextConfig = { // 랜더링 두번 방지 (켜는게 좋긴함 개발 환경에서는. 그리고 strict mode는 배포 환경에서는 동작안함.)
//     reactStrictMode: false,
// };

export default nextConfig;
